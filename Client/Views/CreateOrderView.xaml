<Window x:Class="Client.Views.CreateOrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Подтверждение заказа" Height="500" Width="600"
        WindowStartupLocation="CenterOwner" Owner="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ваша сборка:" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding SelectedComponents}" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Тип" Width="120" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price, StringFormat={}{0:C}}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Grid.Row="2" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right">
            <Run Text="Итоговая стоимость:"/>
            <Run Text="{Binding TotalPrice, StringFormat={}{0:C}, Mode=OneWay}"/>
        </TextBlock>

        <Button Grid.Row="3" Content="Подтвердить заказ" Command="{Binding ConfirmOrderCommand}" 
                Height="40" Margin="0,15,0,0" FontSize="16" FontWeight="Bold"/>
    </Grid>
</Window>