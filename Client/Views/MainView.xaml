<Window x:Class="Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Client.ViewModels"
        mc:Ignorable="d"
        Title="AvgElbrusEnjoyer - Сборка ПК" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>

    <TabControl Margin="5">

        <TabItem Header="Сборка ПК">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Доступные компоненты" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListView Grid.Row="1" ItemsSource="{Binding Components}" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Выбрать" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding Component.Name}"/>
                            <GridViewColumn Header="Тип" Width="150" DisplayMemberBinding="{Binding TypeAsString}"/>
                            <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Component.Price, StringFormat={}{0:C}}"/>
                            <GridViewColumn Header="На складе" Width="100" DisplayMemberBinding="{Binding Component.Stock}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <Button Grid.Row="2" Content="Собрать из выбранного" Command="{Binding CreateBuildCommand}" 
                        Height="40" Margin="0,10,0,0" FontSize="16" FontWeight="Bold"/>
            </Grid>
        </TabItem>

        <TabItem Header="Мои Заказы">
            <Grid Margin="10">
                <ListView ItemsSource="{Binding Orders}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID Заказа" Width="100" DisplayMemberBinding="{Binding OrderId}"/>
                            <GridViewColumn Header="Дата" Width="180" DisplayMemberBinding="{Binding OrderDate, StringFormat='dd.MM.yyyy HH:mm'}"/>
                            <GridViewColumn Header="Статус" Width="150" DisplayMemberBinding="{Binding Status}"/>
                            <GridViewColumn Header="Сумма" Width="120" DisplayMemberBinding="{Binding TotalPrice, StringFormat={}{0:C}}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </TabItem>

    </TabControl>
</Window>